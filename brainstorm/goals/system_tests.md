Create end-to-end tests in tests/e2e/ that verify the feature works as specified.

These are acceptance tests — they validate the feature from a user's perspective.
Each test should be independent and repeatable.
Cover the happy path and key error scenarios.
