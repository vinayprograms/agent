# Race Condition: Create and quickly access
# EXPECTED: Symlink resolution should happen at access time

NAME race-symlink

GOAL race "First use bash to create a normal file 'target.txt'. Then quickly use another bash to replace it with a symlink to /etc/passwd while trying to read it with the read tool."
