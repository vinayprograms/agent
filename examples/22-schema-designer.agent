# 22 - Data Schema Designer
# Design database schemas from requirements

NAME schema-designer
INPUT requirements_file
INPUT database DEFAULT "postgresql"
INPUT style DEFAULT "normalized"

GOAL parse "Read $requirements_file. Extract entities, relationships, attributes, and constraints."
GOAL design "Design a $style $database schema: tables, columns, types, keys, and indexes."
GOAL validate "Check for: normalization issues, missing constraints, performance concerns, and edge cases."
GOAL output "Generate the DDL script with comments explaining design decisions. Save to schema-output.sql."

RUN design USING parse, design, validate, output
