# 18 - Git Commit Analyzer
# Analyze git history and generate insights

NAME git-analyzer
INPUT repo_path DEFAULT "."
INPUT period DEFAULT "30 days"

GOAL gather "Run: git log --oneline (last 20), git shortlog -sn for $period in $repo_path. Minimal output."
GOAL analyze "Extract: commit count, top 3 contributors, top 5 changed files."
GOAL insights "3-5 key insights. Brief recommendations. Save to git-analysis-output.md."

RUN analyze USING gather, analyze, insights
