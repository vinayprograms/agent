NAME acp-compatible-agent
# Version in manifest.json

# Example designed for ACP (Agent Client Protocol) integration
# Works with editors like VS Code, Zed, JetBrains via ACP

INPUT query
INPUT context DEFAULT ""

AGENT assistant FROM agents/editor.md

GOAL understand "Parse query: $query. Identify specific ask."
GOAL gather "Check $context. Minimal tool use unless necessary."
GOAL respond "Concise response with code if needed. Under 200 words." USING assistant
GOAL format "Format for editor. Code blocks only where helpful."

RUN main USING understand, gather, respond, format
