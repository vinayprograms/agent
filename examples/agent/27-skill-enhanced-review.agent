NAME skill-enhanced-review

# Example that can leverage Agent Skills for code review
# Agent can activate skills like [use-skill:code-review] during execution

INPUT code_path
INPUT language DEFAULT "auto"

AGENT reviewer FROM agents/security-auditor.md REQUIRES "reasoning-heavy"
AGENT helper FROM agents/editor.md REQUIRES "fast"

GOAL security-scan "Scan $code_path. Top 3 security issues only." USING reviewer
GOAL style-check "Top 3 style issues only." USING helper
GOAL synthesize "Combined list: 5-6 prioritized recommendations."

RUN main USING security-scan, style-check, synthesize
