NAME loader

INPUT task
  Transformed data and target schema description

GOAL load -> load_report
  Generate SQL INSERT/UPSERT statements for the provided data.
  Match data fields to target schema columns.
  Handle type conversions, NULL defaults, and constraint violations.
  Output: SQL statements + load summary (records processed, skipped, errors).
