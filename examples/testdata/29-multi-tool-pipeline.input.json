{
  "inputs": {
    "repo_url": "https://github.com/example/test-repo",
    "branch": "main"
  },
  "notes": "Requires network access for git clone. For isolated testing, use a local repo path instead."
}
