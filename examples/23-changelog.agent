# 23 - Changelog Generator
# Generate changelogs from git history

NAME changelog
INPUT from_tag
INPUT to_tag DEFAULT "HEAD"
INPUT format DEFAULT "keepachangelog"

GOAL gather "Run: git log --oneline $from_tag..$to_tag. Get commit messages only."
GOAL categorize "Group into: Added, Changed, Fixed. Skip empty categories."
GOAL format "Format as $format. Keep entries brief."
GOAL write "Write CHANGELOG.md."

RUN generate USING gather, categorize, format, write
